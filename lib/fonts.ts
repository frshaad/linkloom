import { Inter, Lobster, Righteous } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const lobster = Lobster({ subsets: ['latin'], weight: ['400'] });
export const righteous = Righteous({ subsets: ['latin'], weight: ['400'] });
